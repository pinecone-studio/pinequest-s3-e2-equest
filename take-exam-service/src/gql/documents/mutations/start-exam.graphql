mutation StartExam(
  $testId: String!
  $studentId: String!
  $studentName: String!
) {
  startExam(
    testId: $testId
    studentId: $studentId
    studentName: $studentName
  ) {
    ...StartExamPayloadFields
  }
}
