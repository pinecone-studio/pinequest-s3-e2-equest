mutation SubmitAnswers(
  $attemptId: String!
  $answers: [AnswerInput!]!
  $finalize: Boolean!
) {
  submitAnswers(
    attemptId: $attemptId
    answers: $answers
    finalize: $finalize
  ) {
    ...SubmitAnswersPayloadFields
  }
}
